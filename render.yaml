services:
  - type: web
    name: astradio-api
    env: node
    rootDir: apps/api
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter api build
    startCommand: node dist/app.js
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: WEB_ORIGIN
        sync: false