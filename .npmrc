# Ensure npm install is used instead of npm ci
package-lock=true
save-exact=true

# Workspace configuration
workspaces-update=false

# Production settings
production=false

# Registry settings
registry=https://registry.npmjs.org/

# Security settings
audit=false
fund=false 

strict-peer-dependencies=false
auto-install-peers=true 