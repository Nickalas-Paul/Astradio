{
  "name": "astradio",
  "version": "1.0.0",
  "description": "Astrological Music Generator",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/api && npm run dev",
    "start": "cd apps/api && npm start",
    "build": "npm run build:packages && cd apps/api && npm run build",
    "build:packages": "cd packages/types && npm run build && cd ../astro-core && npm run build && cd ../audio-mappings && npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
