{
  "name": "astradio",
  "version": "1.0.0",
  "description": "Astrological Music Generator",
  "scripts": {
    "dev": "cd apps/api && npm run dev",
    "start": "cd apps/api && npm start",
    "build": "cd apps/api && npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
