{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd apps/web && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "apps/web/.next/server/**/*.js": {
      "maxDuration": 30
    }
  }
}