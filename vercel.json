{
  "framework": "nextjs",
  "buildCommand": "pnpm -w build && pnpm -F @astradio/web build",
  "installCommand": "corepack enable && pnpm -v || npm i -g pnpm && pnpm -w install --frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}