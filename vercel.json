{
  "framework": "nextjs",
  "installCommand": "pnpm i --frozen-lockfile",
  "buildCommand": "cd apps/web && pnpm install --frozen-lockfile && pnpm build",
  "ignoreCommand": "echo 'Skipping typecheck and lint for deployment'",
  "outputDirectory": "apps/web/.next"
}